const formRef = document.getElementById('search-form');
const inputRef = formRef.querySelector('.search__input');
const gallery = document.querySelector('.collection');

export { formRef, inputRef, gallery };